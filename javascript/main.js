console.log("Hello, World!");
console.log("Estamos na nossa aula 06!");

// Variáveis (const, var, let)
// ! Não utilize o var no seus códigos.
const texto = "Mensagem"; // String
const numero = 10; // Number
const estudando = true; // Boolean
const indefinido = undefined; // Undefined

console.log(typeof texto);
console.log(typeof numero);
console.log(typeof estudando);
console.log(typeof indefinido);

// Operadores matemáticos
const nota1 = 10;
const nota2 = 8;
const media = (nota1 + nota2) / 2;
console.log("A média das notas resulta em: " + media);

// Math
const pi = Math.PI;
console.log(pi); // 3.14
console.log(Math.random() * 100);
console.log(Math.floor(Math.random() * 100));
console.log(Math.round(2737.23762));
